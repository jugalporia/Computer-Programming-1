#include <stdio.h>
void main(){
int a,b;
 printf("Enter a and b:\n");
 scanf ("%d",&a);
 scanf("%d",&b);
 if (a>b)
 printf("A = %d is largest of the two numbers and B = %d is the smallest of two numbers",a,b);
else
printf("B = %d is largest of the two numbers and A = %d is the smallest of two numbers",b,a);
}
